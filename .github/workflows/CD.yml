name: CD on the self-hosted server

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: [ self-hosted ]
    steps:
      - name: Run the script
        uses: actions/checkout@v2
        run: ./deploy.sh
